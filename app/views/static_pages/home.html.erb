<div class="navbar">
  <h2>do?</h2>
</div>
<tr>
  <% @dodesu.each do |dodesu| %>
      <td><%= link_to image_tag(thumbnail_image_dodesu_path(dodesu)), dodesu_path(dodesu) %></td>
  <% end %>
</tr>
<br>
<div class="navbar">
  <h2>dame</h2>
</div>
<tr>
  <% @dame.each do |dame| %>
      <td><%= link_to image_tag(thumbnail_image_dame_path(dame)), dame_path(dame) %></td>
  <% end %>
</tr>

<br>
<a href="https://github.com/nagane/dodaiweb" target="_brank">source code</a>
<a href="https://github.com/nagane" target="_brank">contact as</a>
<br>
<% if user_signed_in? %>
  sign in now
<% else %>
  <%= link_to "sign in with github", user_omniauth_authorize_path(:github) %>
<% end %>
